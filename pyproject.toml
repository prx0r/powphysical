[project]
name = "powphysical"
version = "0.1.0"
description = "Physical-agent hardware discovery and manufacturing routing"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
